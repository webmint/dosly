### Task 001: Add l10n keys for FAB tooltip and modal title

**Agent**: mobile-engineer
**Files**:
- `lib/l10n/app_en.arb` (modify)
- `lib/l10n/app_de.arb` (modify)
- `lib/l10n/app_uk.arb` (modify)
- `lib/l10n/app_localizations.dart`, `app_localizations_en.dart`, `app_localizations_de.dart`, `app_localizations_uk.dart` (auto-regenerated by `flutter gen-l10n` — do NOT hand-edit)

**Depends on**: None
**Blocks**: 002, 003
**Context docs**: None
**Review checkpoint**: No

**Description**:
Introduce two new localization keys — `medsAddFabTooltip` and `medsAddTitle` —
in all three ARB files, then regenerate the `AppLocalizations` bindings via
`flutter gen-l10n`. Both keys carry the same English/German/Ukrainian copy
("Add medication" / "Medikament hinzufügen" / "Додати ліки") because the
tooltip and the modal title intentionally read identically per spec §3.4.
Adding ARB entries first is the constitution's required order: downstream
widget code in Tasks 002 and 003 will reference `context.l10n.medsAddTitle`
and `context.l10n.medsAddFabTooltip`, which can only resolve once the
generated `AppLocalizations` exposes those getters.

**Change details**:
- In `lib/l10n/app_en.arb`:
  - Add key `"medsAddFabTooltip": "Add medication"` plus a sibling
    `"@medsAddFabTooltip"` description block with `"description":
    "Tooltip for the FAB on the Meds screen that opens the placeholder
    Add-medication modal."`
  - Add key `"medsAddTitle": "Add medication"` plus a sibling
    `"@medsAddTitle"` description block with `"description":
    "Title shown at the top of the placeholder Add-medication modal
    bottom sheet on the Meds screen."`
- In `lib/l10n/app_de.arb`:
  - Add `"medsAddFabTooltip": "Medikament hinzufügen"`
  - Add `"medsAddTitle": "Medikament hinzufügen"`
  - Do NOT add `@`-description blocks (DE is a translation file, not
    the template — matches the existing convention where `@@locale` is
    the only metadata in `app_de.arb`).
- In `lib/l10n/app_uk.arb`:
  - Add `"medsAddFabTooltip": "Додати ліки"`
  - Add `"medsAddTitle": "Додати ліки"`
  - Do NOT add `@`-description blocks (same reasoning).
- Run `flutter gen-l10n` (or `flutter pub get` which triggers it via
  `l10n.yaml`) to regenerate `lib/l10n/app_localizations*.dart`.
  Verify the four generated files now expose `String get
  medsAddFabTooltip` and `String get medsAddTitle` getters.

**Done when**:
- [x] All three ARB files contain `medsAddFabTooltip` and `medsAddTitle`
      with the strings specified in spec §3.4.
- [x] `app_en.arb` contains `@medsAddFabTooltip` and `@medsAddTitle`
      description metadata blocks; `app_de.arb` and `app_uk.arb` do not
      (matching existing convention).
- [x] `flutter gen-l10n` runs successfully with no errors or warnings.
- [x] The generated `app_localizations.dart`, `app_localizations_en.dart`,
      `app_localizations_de.dart`, and `app_localizations_uk.dart` each
      expose `String get medsAddFabTooltip` and `String get
      medsAddTitle` returning the locale-correct values.
- [x] `dart analyze 2>&1 | head -40` reports zero issues.
- [x] `flutter test` continues to pass (no test should break from ARB
      additions — only new keys are introduced).

**Spec criteria addressed**: AC-7, AC-8

## Completion Notes
**Status**: Complete
**Completed**: 2026-04-29
**Files changed**: lib/l10n/app_en.arb, lib/l10n/app_de.arb, lib/l10n/app_uk.arb, lib/l10n/app_localizations.dart (regen), lib/l10n/app_localizations_en.dart (regen), lib/l10n/app_localizations_de.dart (regen), lib/l10n/app_localizations_uk.dart (regen)
**Contract**: Expects 5/5 verified | Produces 7/7 verified
**Notes**: `flutter gen-l10n` ran cleanly. `dart analyze`: zero issues. `flutter test`: 170/170 passed. Code review: APPROVE (one Info-level note that the two keys carry identical values in all locales — this is by design per spec §3.4 placeholder convention; Tasks 002/003 may diverge them later if needed). Generated files position: `medsAddFabTooltip` at line 185 / 51 / 51 / 53 in the generated `app_localizations*.dart` files; `medsAddTitle` at 191 / 54 / 54 / 56. supportedLocales list (de, en, uk) unchanged — MEMORY.md alphabetical-fallback note still applies.

## Contracts

### Expects
- `lib/l10n/app_en.arb` exists and contains `"@@locale": "en"` plus the
  existing keys (`bottomNavMeds`, etc.).
- `lib/l10n/app_de.arb` exists and contains `"@@locale": "de"`.
- `lib/l10n/app_uk.arb` exists and contains `"@@locale": "uk"`.
- `l10n.yaml` at the repo root is configured to point `flutter gen-l10n`
  at `lib/l10n/` and emit into `lib/l10n/app_localizations*.dart`.
- The `context.l10n` extension in `lib/l10n/l10n_extensions.dart` exists
  and returns `AppLocalizations`.

### Produces
- `lib/l10n/app_en.arb` defines key `"medsAddFabTooltip"` with value
  `"Add medication"` and a sibling `"@medsAddFabTooltip"` description block.
- `lib/l10n/app_en.arb` defines key `"medsAddTitle"` with value
  `"Add medication"` and a sibling `"@medsAddTitle"` description block.
- `lib/l10n/app_de.arb` defines `"medsAddFabTooltip": "Medikament
  hinzufügen"` and `"medsAddTitle": "Medikament hinzufügen"`.
- `lib/l10n/app_uk.arb` defines `"medsAddFabTooltip": "Додати ліки"` and
  `"medsAddTitle": "Додати ліки"`.
- `lib/l10n/app_localizations.dart` (generated) declares abstract
  `String get medsAddFabTooltip` and `String get medsAddTitle` on the
  `AppLocalizations` class.
- `lib/l10n/app_localizations_en.dart` (generated) overrides
  `medsAddFabTooltip => 'Add medication'` and
  `medsAddTitle => 'Add medication'`.
- `lib/l10n/app_localizations_de.dart` (generated) overrides
  `medsAddFabTooltip => 'Medikament hinzufügen'` and
  `medsAddTitle => 'Medikament hinzufügen'`.
- `lib/l10n/app_localizations_uk.dart` (generated) overrides
  `medsAddFabTooltip => 'Додати ліки'` and `medsAddTitle => 'Додати ліки'`.
