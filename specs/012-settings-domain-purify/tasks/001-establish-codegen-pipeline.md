### Task 001: Establish freezed/build_runner codegen pipeline

**Agent**: architect
**Files**:
- `pubspec.yaml` (modify — add `freezed_annotation`, `freezed`, `build_runner`)
- `pubspec.lock` (auto-regenerated by `flutter pub get` — do NOT hand-edit)
- `analysis_options.yaml` (modify — bring into constitution §7.4 compliance; addendum 2026-04-30 after Task 001 Phase 3.2 surfaced that the bare Flutter default lacks the `analyzer:` block + `**/*.freezed.dart` exclude that constitution §7.4 prescribes and Task 003 requires)

**Depends on**: None
**Blocks**: 003
**Context docs**: None
**Review checkpoint**: Yes (first codegen pipeline in the codebase — verify before downstream tasks build on it)

**Description**:
Bring up the `freezed` + `build_runner` codegen pipeline on the unchanged
codebase as a fail-fast smoke test. This task adds the three packages
mandated by constitution §7.3 (`freezed_annotation` runtime, `freezed` +
`build_runner` dev) and confirms `dart run build_runner build` exits clean
on a codebase that has zero `@freezed` annotations yet. No source files
under `lib/` change in this task — its purpose is to verify the tooling
works before any downstream task depends on it.

This task is risk-rated **High** because:
1. It is the first `*.freezed.dart`-generating pipeline in the codebase (no prior `build_runner` invocation has ever run here).
2. The existing `flutter gen-l10n` pipeline is independent (different tool, different output paths) but they share the `flutter pub get` resolution step — version conflicts surface here.
3. Failure here stops the entire spec; success is a green light for Task 003's `@freezed` migration.

The exact `flutter pub add` form is the canonical Context7 line:
```
flutter pub add freezed_annotation dev:freezed dev:build_runner
```

**Change details**:
- Run `flutter pub add freezed_annotation dev:freezed dev:build_runner`
  from the repo root. Do NOT manually edit `pubspec.yaml` to add these
  entries (constitution §2.3: "Always use `flutter pub add <package>` —
  NEVER edit `pubspec.yaml` manually").
- Verify `pubspec.yaml` now lists:
  - `freezed_annotation: ^<resolved-version>` under `dependencies:`
  - `freezed: ^<resolved-version>` under `dev_dependencies:`
  - `build_runner: ^<resolved-version>` under `dev_dependencies:`
- **(ADDENDUM 2026-04-30)** Bring `analysis_options.yaml` into constitution §7.4
  compliance. The bare Flutter default in this repo lacks the `analyzer:`
  block entirely, which means `**/*.freezed.dart` is NOT excluded and
  Task 003's generated `app_settings.freezed.dart` would trigger lint
  noise. Replace the file with the constitution §7.4 prescribed content:
  the `flutter_lints/flutter.yaml` include, `analyzer:` block with
  `strict-casts/strict-inference/strict-raw-types`, the
  `invalid_annotation_target: ignore` errors override (required for freezed),
  the `**/*.g.dart` + `**/*.freezed.dart` excludes, and the linter rules
  list. **Do NOT enable rules that would create false positives on the
  current codebase** — the bare project hasn't run with strict mode before;
  if any of the prescribed `linter.rules` would flag existing code as
  violations, drop them from this task's scope (note in completion notes)
  and address per-violation in a follow-up. Verify with `dart analyze`
  immediately after the file change: zero issues required.
- Run `dart run build_runner build --delete-conflicting-outputs` to smoke-test
  the pipeline on the unchanged codebase. Expected outcome: build_runner
  reports zero generated files (because no `@freezed`/`@JsonSerializable`/
  etc. annotation exists in the codebase yet) and exits 0.
- Run `dart analyze 2>&1 | head -40`. Expected: zero issues — the new
  packages added imports nowhere yet, so the analyzer should be unaffected.

**Done when**:
- [x] `pubspec.yaml` lists all three new packages (`freezed_annotation`,
      `freezed`, `build_runner`) at the resolved minor versions chosen by
      `flutter pub add`.
- [x] `pubspec.lock` is regenerated and consistent.
- [x] `dart run build_runner build --delete-conflicting-outputs` exits 0
      with no generated files written under `lib/`.
- [x] `dart analyze 2>&1 | head -40` reports zero issues.
- [x] `flutter test` continues to pass (no source change should affect any test).
- [x] `analysis_options.yaml` `analyzer.exclude` lists
      `**/*.freezed.dart` (verified after the analyzer §7.4 addendum applied this task).

**Spec criteria addressed**: AC-11

## Completion Notes
**Status**: Complete
**Completed**: 2026-04-30
**Files changed**: pubspec.yaml (3 deps via `flutter pub add`), pubspec.lock (16 transitive packages added), analysis_options.yaml (replaced bare Flutter default with constitution §7.4 compliant config)
**Contract**: Expects 4/4 verified | Produces 5/5 verified
**Notes**:
- Resolved versions: `freezed_annotation: ^3.1.0` (runtime), `freezed: ^3.2.5` (dev), `build_runner: ^2.15.0` (dev). Version family alignment between freezed and freezed_annotation (both 3.x major) — required.
- `dart run build_runner build --delete-conflicting-outputs` exited 0 with `freezed on 44 inputs: 44 no-op` and `wrote 0 outputs` — correct (no `@freezed` annotations exist yet).
- `build_runner 2.15.0` warns that `--delete-conflicting-outputs` is now a no-op (the behavior is the new default). Future invocations can drop the flag, but leaving it in scripts is harmless.
- **Constitution drift discovered + fixed in addendum**: `analysis_options.yaml` was the bare Flutter default — no `analyzer:` block at all, contradicting constitution §7.4 + the original task's "verification only, no edit" assumption. The addendum brought it into compliance with strict-casts/strict-inference/strict-raw-types language modes, `invalid_annotation_target: ignore` (required for freezed), exclude globs for `**/*.g.dart` and `**/*.freezed.dart`, and 18 of the 20 prescribed `linter.rules`.
- **2 lint rules deferred** (commented out with rationale in `analysis_options.yaml`):
  - `directives_ordering` — produces 4 issues in `test/` files; deferred to a future lint-hardening pass that has scope to touch `test/`.
  - `sort_pub_dependencies` — `flutter pub add` appended `freezed_annotation` to the bottom of `dependencies:` and `flutter_lints` to the bottom of `dev_dependencies:` without sorting; constitution §2.3 forbids hand-editing pubspec.yaml. Defer until a canonical-order `flutter pub add` reshuffle pass.
- Code review verdict: APPROVE with 5 warnings.
  - W1+W2 (`riverpod_annotation`/`riverpod_generator` missing): **out of scope for spec 012** per plan §"Constitution Compliance" row §4.1.1 — manual `Provider`/`NotifierProvider` declarations stay; only their parameter types change (bug 004 territory). No `@riverpod` annotations land in this spec, so neither package is needed. Recorded here so the reviewer's misread doesn't surface again.
  - W3 (`directives_ordering` comment "Rule 2" reference): fixed in polish — comment now standalone-readable with full deferral rationale.
  - W4 (`sort_pub_dependencies` comment misleading): fixed in polish — comment now correctly attributes the deferral to `flutter pub add` append behavior + §2.3.
  - W5 (stale "no edit needed" parenthetical in Done-When): fixed in polish — now reads "verified after the analyzer §7.4 addendum applied this task".
- `flutter test`: 184/184 passing throughout (smoke after pubspec change, smoke after analyzer config, smoke after polish).
- Zero source files under `lib/` were modified — scope discipline maintained.

## Contracts

### Expects
- `pubspec.yaml` exists at the repo root with the current dependency set
  (`flutter`, `flutter_riverpod`, `shared_preferences`, `fpdart`, etc.).
- `analysis_options.yaml` exists and includes `**/*.freezed.dart` under
  `analyzer.exclude` (per constitution §7.4).
- The Flutter SDK in `environment.sdk: ^3.11.1` is current enough to
  resolve modern `freezed`/`build_runner` versions.
- No `*.freezed.dart` files exist anywhere under `lib/`.

### Produces
- `pubspec.yaml` lists `freezed_annotation` under `dependencies:` with a
  caret-pinned version chosen by `flutter pub add`.
- `pubspec.yaml` lists `freezed` and `build_runner` under
  `dev_dependencies:` with caret-pinned versions chosen by `flutter pub add`.
- `pubspec.lock` is regenerated and contains entries for `freezed_annotation`,
  `freezed`, `build_runner`, and their transitive dependencies.
- `dart run build_runner build --delete-conflicting-outputs` exits 0 on
  the unchanged source tree (smoke test — no generated output expected).
- No source files under `lib/` are modified by this task.
