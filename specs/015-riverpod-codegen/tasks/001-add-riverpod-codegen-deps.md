### Task 001: Add `riverpod_annotation` + `riverpod_generator` to pubspec

**Agent**: architect
**Files**:
- `pubspec.yaml` (modify — via `flutter pub add`; add `riverpod_annotation` to `dependencies` and `riverpod_generator` to `dev_dependencies`)
- `pubspec.lock` (auto-regenerated by `flutter pub get` — do NOT hand-edit)

**Depends on**: None
**Blocks**: 002, 003, 004
**Context docs**: None
**Review checkpoint**: Yes (first `riverpod_generator` integration — version compatibility with the pinned `flutter_riverpod ^3.3.1` is the only real risk surface; verify before downstream tasks build on it)

**Description**:

Add the two packages mandated by constitution §1 ("Riverpod 2.x with `riverpod_generator`") and §4.1.1 ("Always use `@riverpod` codegen") that are currently missing from `pubspec.yaml`. The `build_runner ^2.15.0` toolchain is already present (added by feature 012 for `freezed`); this task extends the same pipeline with the Riverpod codegen builder.

No source files under `lib/` change in this task. Its purpose is to verify that `flutter pub add` resolves both packages to versions compatible with `flutter_riverpod ^3.3.1` and that `dart run build_runner build` continues to exit clean on a codebase that has zero `@riverpod` annotations yet.

The exact `flutter pub add` invocations (constitution §2.3 forbids manual `pubspec.yaml` edits for deps):

```
flutter pub add riverpod_annotation
flutter pub add dev:riverpod_generator
```

Two separate commands so the diff is easy to audit.

**Change details**:
- Run `flutter pub add riverpod_annotation` from the repo root. Verify `pubspec.yaml` now lists `riverpod_annotation: ^<resolved-version>` under `dependencies:`.
- Run `flutter pub add dev:riverpod_generator` from the repo root. Verify `pubspec.yaml` now lists `riverpod_generator: ^<resolved-version>` under `dev_dependencies:`.
- Confirm the resolved versions are on the same major line as `flutter_riverpod ^3.3.1` (Riverpod 3.x). If `flutter pub add` resolves either package to a major that conflicts with `flutter_riverpod ^3.3.1`, stop and reconcile — likely by upgrading `flutter_riverpod` and/or pinning the trio explicitly.
- Run `dart run build_runner build --delete-conflicting-outputs` to smoke-test the pipeline on the unchanged codebase. Expected outcome: the `riverpod_generator` builder reports zero `@riverpod` annotations found and exits 0; the existing `freezed` builder still emits `app_settings.freezed.dart` correctly.
- Run `dart analyze 2>&1 | head -40`. Expected: zero new issues — the new packages add imports nowhere yet, so the analyzer should be unaffected.
- Run `flutter test`. Expected: full suite continues to pass (no source change should affect any test).
- Do NOT modify any source files under `lib/`. Do NOT modify `analysis_options.yaml` (already configured to exclude `**/*.g.dart` per `analysis_options.yaml:19-21`).

**Done when**:
- [x] `pubspec.yaml` lists `riverpod_annotation: ^4.0.2` under `dependencies:` (resolved by `flutter pub add`).
- [x] `pubspec.yaml` lists `riverpod_generator: ^4.0.3` under `dev_dependencies:` (resolved by `flutter pub add`).
- [x] `pubspec.lock` is regenerated and contains entries for both packages plus transitive deps.
- [x] `dart run build_runner build --delete-conflicting-outputs` exits 0 with the existing `app_settings.freezed.dart` output preserved and zero new `*.g.dart` files written under `lib/`.
- [x] `dart analyze 2>&1 | head -40` reports zero issues.
- [x] `flutter test` continues to pass (full suite — 203 tests).
- [x] No source file under `lib/` or `test/` is modified by this task.

**Spec criteria addressed**: AC-1

## Completion Notes
**Status**: Complete
**Completed**: 2026-05-09
**Files changed**: pubspec.yaml (2 deps via `flutter pub add`), pubspec.lock (auto-regenerated; 4 transitive packages added: `analyzer_buffer 0.3.0`, `code_builder 4.11.1`, `mockito 5.6.4`, `riverpod_analyzer_utils 1.0.0-dev.9`)
**Contract**: Expects 4/4 verified | Produces 5/5 verified
**Notes**:
- Resolved versions: `riverpod_annotation: ^4.0.2` (runtime), `riverpod_generator: ^4.0.3` (dev). Riverpod project uses offset major versioning — annotation pkg 4.x and `flutter_riverpod` 3.x both resolve to the same underlying `riverpod 3.2.1` core (verified via `pubspec.lock`).
- Pub solver downgraded the analyzer dev-stack to satisfy `riverpod_generator 4.0.3`'s ceiling: `analyzer 10.0.1 → 9.0.0`, `_fe_analyzer_shared 93.0.0 → 92.0.0`, `dart_style 3.1.7 → 3.1.3`. All dev-only; not shipped on device. `dart analyze` passes clean on the downgraded stack.
- `mockito 5.6.4` added as a transitive of `riverpod_generator` (its own test scaffolding). The project test suite still uses `mocktail` exclusively per constitution §3.4 — `mockito` is a dev-time build dep only.
- Expected `--delete-conflicting-outputs` ignored-flag warning from `build_runner 2.15.0` — same as feature 012; flag retained for documentation consistency with constitution §6.6.
- Code review verdict: **APPROVE** with 3 info-level warnings (pre-release transitive `riverpod_analyzer_utils 1.0.0-dev.9`, analyzer-stack downgrade, mockito transitive presence). All warnings are inherent to `riverpod_generator 4.0.3`'s dependency tree and dev-only.
- `flutter test`: 203/203 passing throughout.
- Zero source files under `lib/` or `test/` modified — scope discipline maintained.

## Contracts

### Expects
- `pubspec.yaml` exists at the repo root with `flutter_riverpod: ^3.3.1`, `freezed_annotation: ^3.1.0` under `dependencies:` and `freezed: ^3.2.5`, `build_runner: ^2.15.0` under `dev_dependencies:`.
- `analysis_options.yaml` excludes `**/*.g.dart` and `**/*.freezed.dart` under `analyzer.exclude` (verified at `analysis_options.yaml:19-21`).
- The `dart run build_runner build --delete-conflicting-outputs` command works in this repo (verified by feature 012).
- No `riverpod_annotation` or `riverpod_generator` entries currently exist in `pubspec.yaml`.

### Produces
- `pubspec.yaml` lists `riverpod_annotation` under `dependencies:` with a caret-pinned version chosen by `flutter pub add`, on the Riverpod 3.x major line.
- `pubspec.yaml` lists `riverpod_generator` under `dev_dependencies:` with a caret-pinned version chosen by `flutter pub add`, on the Riverpod 3.x major line.
- `pubspec.lock` contains entries for `riverpod_annotation`, `riverpod_generator`, and any new transitive dependencies.
- `dart run build_runner build --delete-conflicting-outputs` exits 0 on the unchanged source tree (smoke test — only existing freezed output regenerated, no new `*.g.dart` files).
- No source files under `lib/` or `test/` are modified by this task.
