# Task 007: Add Intake-section localization keys (en/de/uk)

**Agent**: mobile-engineer
**Files**: `lib/l10n/app_en.arb`, `lib/l10n/app_de.arb`, `lib/l10n/app_uk.arb`
**Depends on**: None
**Blocks**: 008
**Context docs**: `docs/features/i18n.md`
**Review checkpoint**: No

**Description**:
Add the localization strings for the new "Intake" settings section to all three ARB files (with `@`-descriptions in `app_en.arb`). Includes a `{minutes}`-placeholder value string used to render "N min" for both steppers, and increase/decrease tooltips for accessibility. Provide accurate German and Ukrainian translations (the app already ships de/uk).

**Change details**:
- Add these keys to `app_en.arb` (each with an `@`-description), and matching translations to `app_de.arb` and `app_uk.arb`:
  - `settingsIntakeHeader` — section header, e.g. "Intake".
  - `settingsIntakeWindowLabel` — e.g. "Intake window".
  - `settingsIntakeWindowDescription` — e.g. "How long after the scheduled time a dose can still be marked".
  - `settingsGracePeriodLabel` — e.g. "Grace period".
  - `settingsGracePeriodDescription` — e.g. "How long you can undo a dose after marking it".
  - `settingsAllowMarkAheadLabel` — e.g. "Allow marking ahead".
  - `settingsAllowMarkAheadDescription` — e.g. "Let doses be marked before their window opens".
  - `settingsMinutesValue` — `"{minutes} min"` with an `int` `minutes` placeholder.
  - `settingsStepperIncreaseTooltip` — e.g. "Increase".
  - `settingsStepperDecreaseTooltip` — e.g. "Decrease".
- Keep keys alphabetically/section-grouped consistent with the file's existing convention.

**Contracts**:

### Expects
- `app_en.arb`, `app_de.arb`, `app_uk.arb` exist and drive `AppLocalizations` via `flutter gen-l10n`.

### Produces
- All three ARB files contain `settingsIntakeHeader`, `settingsIntakeWindowLabel`, `settingsIntakeWindowDescription`, `settingsGracePeriodLabel`, `settingsGracePeriodDescription`, `settingsAllowMarkAheadLabel`, `settingsAllowMarkAheadDescription`, `settingsMinutesValue`, `settingsStepperIncreaseTooltip`, `settingsStepperDecreaseTooltip`.
- `settingsMinutesValue` uses a `{minutes}` placeholder.

**Done when**:
- [x] `AppLocalizations` regenerates cleanly; `context.l10n.settingsIntakeHeader` and `context.l10n.settingsMinutesValue(120)` resolve.
- [x] All 10 keys exist in en, de, and uk with no missing-translation warnings.
- [x] `dart analyze` passes.

**Spec criteria addressed**: AC-16

## Completion Notes
**Status**: Complete
**Completed**: 2026-07-03
**Files changed**: `app_en.arb`, `app_de.arb`, `app_uk.arb` (+ regen `app_localizations*.dart`)
**Contract**: Produces [all verified — 10 keys × 3 locales, `{minutes}` int placeholder, `settingsMinutesValue(int minutes)` generated]
**Code review**: APPROVE (3 Info: DE "Kulanzzeit" alt, UK термінологія note, i18n.md doc deferred to /finalize)
**Notes**: Generated l10n sources are checked into git in this project — committed alongside the ARBs.
