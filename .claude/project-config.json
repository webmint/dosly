{
  "PROJECT_NAME": "dosly",
  "PROJECT_TYPE": "Mobile application (iOS + Android)",
  "FRAMEWORK": "Flutter",
  "LANGUAGE": "Dart",
  "BUILD_TOOL": "flutter",
  "BUILD_COMMAND": "flutter build apk",
  "TYPE_CHECK_COMMAND": "dart analyze 2>&1 | head -40",
  "LINT_COMMAND": "dart analyze 2>&1 | head -40",
  "SOURCE_ROOT": ".",
  "PROJECT_MODE": "greenfield",
  "ARCHITECTURE": "Clean Architecture",
  "ERROR_HANDLING": "Either/Result types (fpdart)",
  "API_LAYER": "REST",
  "STATE_MANAGEMENT": "Riverpod",
  "STYLING": "Flutter widgets (Material Design 3 + iOS Cupertino where appropriate)",
  "MONOREPO_TOOL": "N/A",
  "TESTING": "flutter_test + mocktail",
  "PROJECT_PATHS": "- Source: `lib/`\n- Entry point: `lib/main.dart`\n- Core (errors, theme, utilities): `lib/core/`\n- Features: `lib/features/[feature]/{data,domain,presentation}/`\n- Domain layer (pure Dart, no Flutter imports): `lib/features/[feature]/domain/`\n- Tests: `test/` (mirrors `lib/`)\n- iOS native: `ios/`\n- Android native: `android/`\n- Dependencies: `pubspec.yaml`\n- Lint config: `analysis_options.yaml`",
  "PROJECT_STRUCTURE": ".\n├── lib/\n│   ├── main.dart\n│   ├── core/\n│   │   ├── error/\n│   │   ├── theme/\n│   │   ├── routing/\n│   │   └── utils/\n│   └── features/\n│       └── [feature]/\n│           ├── data/\n│           │   ├── datasources/\n│           │   ├── models/\n│           │   └── repositories/\n│           ├── domain/\n│           │   ├── entities/\n│           │   ├── repositories/\n│           │   └── usecases/\n│           └── presentation/\n│               ├── providers/\n│               ├── screens/\n│               └── widgets/\n├── test/\n├── android/\n├── ios/\n├── pubspec.yaml\n└── analysis_options.yaml",
  "DEV_COMMANDS": "- `flutter pub get` — Install dependencies from `pubspec.yaml`\n- `flutter pub upgrade` — Upgrade dependencies to latest allowed versions\n- `flutter run` — Run on the currently connected device/emulator\n- `flutter run -d ios` — Run on iOS simulator\n- `flutter run -d android` — Run on Android emulator\n- `flutter build apk` — Build debug Android APK\n- `flutter build appbundle` — Build Android App Bundle (Play Store)\n- `flutter build ios --no-codesign` — Build iOS without signing (for CI)\n- `flutter test` — Run unit and widget tests\n- `flutter test --coverage` — Run tests with coverage report\n- `dart analyze` — Static analysis: linting + type checking\n- `dart format lib test` — Format Dart code\n- `flutter clean` — Clear build artifacts and pub caches",
  "AGENT_LIST": "- `code-reviewer` — Code review, constitution compliance, Dart type safety\n- `qa-engineer` — Test writing (flutter_test + mocktail), coverage analysis, failure diagnosis\n- `runtime-debugger` — Runtime error diagnosis and minimal-fix loop\n- `tech-writer` — Inline dartdoc and `docs/` folder maintenance\n- `security-reviewer` — OWASP MASVS, secret leaks, mobile auth flows\n- `mobile-engineer` — Flutter screens, navigation, platform channels\n- `architect` — Clean Architecture decisions, use cases, repositories\n- `design-auditor` — Material 3 / Cupertino compliance, accessibility\n- `performance-analyst` — Startup time, frame rate, app size, memory profiling",
  "WRAPPER_MODE_SECTION": "",
  "COMMIT_ATTRIBUTION": "Include AI attribution in every commit by appending this trailer:\n`Co-Authored-By: Claude <noreply@anthropic.com>`",
  "MODEL_THINK": "opus",
  "MODEL_DO": "sonnet",
  "MODEL_VERIFY": "sonnet",
  "AC_VERIFICATION": "off",
  "AC_VERIFICATION_URL": "N/A",
  "AC_VERIFICATION_API_BASE": "N/A",
  "DEFAULT_BRANCH": "main",
  "TYPE_SAFETY_RULES": "- No `dynamic` types without documented justification — prefer specific types or generics\n- Honor null safety: use `?` for nullable, `late` only when initialization is guaranteed before first read\n- Avoid unsafe casts (`as` operator) — use `is` checks or pattern matching\n- No `!` (null assertion) unless the value is provably non-null at the call site\n- Prefer `final`/`const` over mutable variables; immutability is the default"
}
